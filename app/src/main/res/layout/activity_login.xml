<?xml version="1.0" encoding="utf-8"?>
<!--
    Diseño de la pantalla de Inicio de Sesión.
    Usamos un LinearLayout vertical para poner los elementos uno debajo de otro.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layoutLogin"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_vertical">

    <!-- Logo de la aplicación que aparece arriba del todo -->
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:src="@drawable/ic_logo_app" />

    <!-- Título principal de la app -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Agenta"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_marginBottom="32dp"
        android:textColor="#6200EE"/>

    <!-- Espacio donde el usuario escribe su nombre de cuenta -->
    <EditText
        android:id="@+id/etUsuario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nombre de Usuario"
        android:inputType="textPersonName"
        android:layout_marginBottom="16dp"/>

    <!-- Espacio para la clave, oculta los caracteres con puntitos por seguridad -->
    <EditText
        android:id="@+id/etContrasena"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Contraseña"
        android:inputType="textPassword"
        android:layout_marginBottom="24dp"/>

    <!-- Botón principal para entrar a la cuenta -->
    <Button
        android:id="@+id/btnIniciarSesion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Iniciar Sesión"
        android:backgroundTint="#6200EE"/>

    <!-- Botón secundario para los usuarios que aún no tienen cuenta -->
    <Button
        android:id="@+id/btnNuevaSesion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:text="¿No tienes cuenta? Regístrate"
        android:textColor="#6200EE"/>
</LinearLayout>
